{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Narrado",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "preLaunchTask": "build"
    }
  ]
}
